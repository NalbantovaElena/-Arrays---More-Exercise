function solve(input) {
    let countRotate = Number(input.pop())
    for (let i = 0; i < countRotate; i++) {
        let lastElement = input.pop()
        input.unshift(lastElement)
    }
    console.log(input.join(' '))
}

function solve(input) {
    let arr = []
    let number = 0;
    input.forEach(element => {
         number++;
        if (element === 'add') {
           
            arr.push(number)
        } else if (element === 'remove') {
            arr.pop()
        }
    });
    if(arr.length!==0){
        console.log(arr.join(' '))
    }else{
        console.log('Empty')
    }
    
}
